# Database Configuration
# -------------------------------------------------------
db.driver=org.h2.Driver
#db.driver=com.mysql.jdbc.Driver
#db.driver=org.postgresql.Driver

db.url=jdbc:h2:mem:jsca;DB_CLOSE_DELAY=-1
#db.url=jdbc:mysql://localhost:3306/jsca
#db.url=jdbc:postgresql://localhost/datajpa

db.username=as
db.password=

db.dialect=org.hibernate.dialect.H2Dialect
#db.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
#db.dialect=org.hibernate.dialect.PostgreSQLDialect
db.show_sql=false
db.generate_ddl=false

# Hibernate Configuration
# -------------------------------------------------------
#hibernate.format_sql=true
hibernate.hbm2ddl.auto=create
#update|create|create-drop|validate (valores posibles)

# EntityManager
# -------------------------------------------------------
#Declares the base package of the entity classes (character , several packages).
packages.to.scan=entities